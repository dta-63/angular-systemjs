'use strict';
/**
 * Application states using ui-router
 */
import app from 'app/app.config';

/**
 * Represents the application states configuration block.
 * @constructor
 */
function AppStates(stateProvider) {
   
}

// define AppConfig injection parameters
AppStates.$inject = ['$stateProvider'];
